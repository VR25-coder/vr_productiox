# Example environment configuration for vr-portfolio
# Copy this file to .env and fill in real values before running in production.

# HTTP port the Express server listens on
PORT=3002

# Environment: use "production" for deployed environments, "development" locally
NODE_ENV=development

# Initial admin password used on first run.
# The server will hash this into data/admin_auth.json.
# After that, you can change the password from the Admin Panel (Security tab).
ADMIN_PASSWORD=change_me_to_a_strong_password

# JWT signing secret (use a long random string; keep at least 32 characters)
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars

# Optional SMTP configuration for contact-form email notifications
# These settings let the backend send a confirmation email to you and to visitors
# after they submit the contact form. Use any SMTP provider with a free tier
# (for example: Gmail SMTP, Resend, Mailjet, etc.).
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# From address used for all outgoing mail (e.g. "Vrutant Portfolio <no-reply@example.com>")
MAIL_FROM="Vrutant Portfolio <no-reply@example.com>"

# Where admin copies of contact-form messages should be delivered
MAIL_TO=owner@example.com
